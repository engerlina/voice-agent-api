[build]
builder = "dockerfile"
# dockerfilePath is set via RAILWAY_DOCKERFILE_PATH env var per service

[deploy]
# startCommand, healthcheckPath, and internalPort are set per-service via Railway variables
# or in their respective Dockerfiles
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
