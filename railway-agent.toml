[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.agent"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1

[service]
# LiveKit agent internal HTTP server
internalPort = 8081
